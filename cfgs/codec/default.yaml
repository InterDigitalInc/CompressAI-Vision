type: "bypass"
eval_encode: "bpp" # or "bitrate" 
output_dir: "${pipeline.output_dir_root}/${.type}/${dataset.datacatalog}/${dataset.config.datasetname}"
bitstream_name: "${dataset.config.dataset_name}"
encoder_config:
  qp: 'uncmp'
  nbit_quant: -1
